import { ContextTestDirective } from './context-test.directive';

describe('ContextTestDirective', () => {
  it('should create an instance', () => {
    const directive = new ContextTestDirective();
    expect(directive).toBeTruthy();
  });
});
