import { FormPersistenceDirective } from './form-persistence.directive';

describe('FormPersistenceDirective', () => {
  it('should create an instance', () => {
    const directive = new FormPersistenceDirective();
    expect(directive).toBeTruthy();
  });
});
