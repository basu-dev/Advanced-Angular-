import { HeightCalculatePipe } from './height-calculate.pipe';

describe('HeightCalculatePipe', () => {
  it('create an instance', () => {
    const pipe = new HeightCalculatePipe();
    expect(pipe).toBeTruthy();
  });
});
