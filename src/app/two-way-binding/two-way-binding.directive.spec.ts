import { TwoWayBindingDirective } from './two-way-binding.directive';

describe('TwoWayBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new TwoWayBindingDirective();
    expect(directive).toBeTruthy();
  });
});
